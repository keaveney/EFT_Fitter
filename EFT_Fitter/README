1)macro for extracting constraint on ctg using measured differential cross section,
corrsponding covariance matrix and appropriate "basis", predicitons.

Inputs are hardcoded:


Compile and run as follows:
clang++ -o bin/fit src/fit.cpp `root-config --cflags --libs` -lRooFit -lRooFitCore
./bin/fit

2)


//macro for plot editing and chi2 tables
clang++ -o plot src/make_pvals.cc  `root-config --cflags --libs` -I /usr/local/include -I/usr/local/include -L/usr/local/lib -lboost_system -lRooFit -lRooFitCore



